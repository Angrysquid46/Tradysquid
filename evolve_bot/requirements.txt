# Fully isolated from .venv-tradysquid/requirements.txt on purpose - the
# live trading system's environment must never be touched by this bot's
# dependencies. requests/tzdata are duplicated here (not shared) so this
# venv has zero import-time dependency on the live checkout's venv.
requests==2.32.4
tzdata==2025.2
pandas>=2.2,<3
numpy>=1.26,<3
lightgbm>=4.5,<5
shap>=0.46,<1
