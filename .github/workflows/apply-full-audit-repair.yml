name: Apply full audit repair

on:
  pull_request:
    branches:
      - clean-rebuild
    paths:
      - ".github/full-audit-repair-trigger"

permissions:
  contents: write

jobs:
  repair-and-verify:
    if: github.head_ref == 'trigger-full-audit-repair'
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Check out clean rebuild
        uses: actions/checkout@v4
        with:
          ref: clean-rebuild
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Apply audited repairs
        shell: pwsh
        run: |
          python scripts/apply_full_audit_repairs.py
          python scripts/apply_full_audit_followup.py
          Remove-Item -LiteralPath scripts/apply_full_audit_repairs.py -Force
          Remove-Item -LiteralPath scripts/apply_full_audit_followup.py -Force
          Remove-Item -LiteralPath .github/workflows/apply-full-audit-repair.yml -Force

      - name: Parse every PowerShell script
        shell: pwsh
        run: |
          $Failures = @()
          Get-ChildItem -Recurse -Filter '*.ps1' | ForEach-Object {
            $Tokens = $null
            $Errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile(
              $_.FullName,
              [ref]$Tokens,
              [ref]$Errors
            )
            foreach ($ErrorRecord in $Errors) {
              $Failures += "$($_.FullName):$($ErrorRecord.Extent.StartLineNumber):$($ErrorRecord.Extent.StartColumnNumber) $($ErrorRecord.Message)"
            }
          }
          if ($Failures.Count -gt 0) {
            $Failures | ForEach-Object { Write-Host $_ }
            throw 'PowerShell parsing failed.'
          }

      - name: Create isolated production virtual environment
        shell: pwsh
        run: |
          py -3.12 -m venv .venv-tradysquid
          $Python = Join-Path (Get-Location) '.venv-tradysquid\Scripts\python.exe'
          if (!(Test-Path -LiteralPath $Python -PathType Leaf)) {
            throw 'Isolated interpreter was not created.'
          }
          & $Python --version

      - name: Install dependencies
        shell: pwsh
        run: |
          $Python = Join-Path (Get-Location) '.venv-tradysquid\Scripts\python.exe'
          & $Python -m pip install --upgrade pip
          if ($LASTEXITCODE -ne 0) { throw 'pip upgrade failed.' }
          & $Python -m pip install -r requirements-dev.txt
          if ($LASTEXITCODE -ne 0) { throw 'Dependency installation failed.' }
          & $Python -m pip install --editable .
          if ($LASTEXITCODE -ne 0) { throw 'Editable installation failed.' }
          & $Python -m pip check
          if ($LASTEXITCODE -ne 0) { throw 'Dependency integrity failed.' }

      - name: Compile source
        shell: pwsh
        run: |
          $Python = Join-Path (Get-Location) '.venv-tradysquid\Scripts\python.exe'
          & $Python -m compileall -q tradysquid scripts tests
          if ($LASTEXITCODE -ne 0) { throw 'Compilation failed.' }

      - name: Run complete suite
        shell: pwsh
        run: |
          $Python = Join-Path (Get-Location) '.venv-tradysquid\Scripts\python.exe'
          & $Python -m pytest
          if ($LASTEXITCODE -ne 0) { throw 'Tests failed.' }

      - name: Run production installation verifier
        shell: pwsh
        run: |
          $Python = Join-Path (Get-Location) '.venv-tradysquid\Scripts\python.exe'
          & $Python -m scripts.verify_installation
          if ($LASTEXITCODE -ne 0) { throw 'Installation verification failed.' }

      - name: Commit verified repairs
        shell: pwsh
        run: |
          git config user.name "Tradysquid Audit Repair"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          if (git diff --cached --quiet) {
            throw 'Audit repair produced no repository changes.'
          }
          git commit -m "Repair full runtime audit findings"
          git push origin HEAD:clean-rebuild
